package com.kami.study.finalProject.service.checker.impl;

import com.kami.study.finalProject.model.enums.CommissionType;
import com.kami.study.finalProject.service.checker.Checker;

public class PaymentSystemCheckerImpl implements Checker<String> {

    // todo: implement later if you have time. same as bank checker.

    @Override
    public boolean check(String uncheckedData) {
        return true;
    }
}
